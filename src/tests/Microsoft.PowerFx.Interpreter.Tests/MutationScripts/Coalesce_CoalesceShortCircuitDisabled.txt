#SETUP: disable:CoalesceShortCircuit

// Cases to test how shortcut verification work along with behavior functions

>> Set( t1, [1,2] )
Table({Value:1},{Value:2})

>> Coalesce(1,Clear(t1))
1

>> 1;t1
Table()

>> 1;Set( t1, [1,2,3] )
Table({Value:1},{Value:2},{Value:3})

>> Coalesce(1,Blank(),Clear(t1))
1

>> 1.1;t1
Table()

>> 2;Set( t1, [1,2,3,3.1] )
Table({Value:1},{Value:2},{Value:3},{Value:3.1})

>> Set( bn, If(1<0,1))
Blank()

>> Coalesce( bn, 1.1 )
1.1

>> Coalesce( bn, Remove(t1, First(t1));Last(t1).Value, Remove(t1, First(t1));Last(t1).Value )
3.1

>> 3;t1
Table({Value:3},{Value:3.1})

>> Coalesce( bn, Blank(), Remove(t1, First(t1));Last(t1).Value )
3.1

>> 4;t1
Table({Value:3.1})

>> 3;Set( t1, [1,2,3,3.1] )
Table({Value:1},{Value:2},{Value:3},{Value:3.1})

>> Coalesce( bn, Blank(), Remove(t1, First(t1));First(t1).Value, Remove(t1, First(t1));Last(t1).Value )
2

>> 5;t1
Table({Value:3},{Value:3.1})

