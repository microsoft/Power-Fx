#SETUP: PowerFxV1CompatibilityRules

// BASICS

>> Set( x, {a:1, b:2})
void

>> Set( y, x )
void

>> Set( x.a, 3 )
If(true, {test:1}, "Void value (result of the expression can't be used).")

>> x // after set( x.a, ... )
{a:3,b:2}

>> y // after set( x.a, ... )
{a:1,b:2}

>> Set( y.b, 4 )
If(true, {test:1}, "Void value (result of the expression can't be used).")

>> x // after set( y.b, ... )
{a:3,b:2}

>> y // after set( y.b, ... )
{a:1,b:4}

>> Set( {a:1,b:2}.a, 3 )
Error 0-21: First argument to this function cannot be modified.

// FUNCTIONS THAT PROPOGATE MUTABILITY

>> Set( a, [1,2,3] )
void

>> Set( b, a )
void

>> Set( First(a).Value, 4 )
void

>> a // after Set( First(a).Value, 4 )
void

>> b // after Set( First(a).Value, 4 )
void

>> Set( Last(b).Value, 5 )
void

>> a // after Set( Last(b).Value, 5 )
void

>> b // after Set( Last(b).Value, 5 )
void

